{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/consepopgen/master/assets/schema_input.json",
    "title": "nf-core/consepopgen pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "Individual": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Individual name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "Population": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Population name must be provided and cannot contain spaces"
            },
            "VCF": {
                "type": "string",
                "format": "string",
                "pattern": "^\\S+\\.vcf(\\.gz)?$",
                "errorMessage": "VCF file must be provided, cannot contain spaces and must have extension '.vcf' or '.vcf.gz'"
            }
        },
        "required": ["Individual", "Population", "VCF"]
    }
}
